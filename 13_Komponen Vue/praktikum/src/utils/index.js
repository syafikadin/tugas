export const penambahan = (var1, var2) => {
    return var1 + var2;
};

export const addArray = (message) => {
    if (message == "") {
      alert("Data yang anda masukkan kosong. Isi dengan benar!")
    }
    else{
      this.toDoList.push(message)
      this.message = ""
    }
};